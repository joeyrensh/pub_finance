import base64
from utility.MyEmail import MyEmail
from utility.FileInfo import FileInfo
import pandas as pd
import seaborn as sns
import os
import sys
from numpy import size
from pyspark.sql import SparkSession
import matplotlib.pyplot as plt
from matplotlib.pylab import mpl
import matplotlib.font_manager as fm
import plotly.graph_objects as go
import plotly.express as px
from datetime import datetime, timedelta
from email.mime.text import MIMEText
import plotly.io as pio
import pandas as pd
import plotly.graph_objects as go
import time
import requests
import re
import json
import pandas as pd
from datetime import datetime

url = (
    "http://23.push2.eastmoney.com/api/qt/clist/get?cb=jQuery&pn=i&pz=20000"
    "&po=1&np=1&ut=&fltt=2&invt=2&fid=f3&fs=m:market"
    "&fields=f2,f3,f4,f5,f6,f7,f12,f14,f15,f16,f17,f18&_=unix_time"
)
# """url里需要传递unixtime当前时间戳"""
current_timestamp = int(time.mktime(datetime.now().timetuple()))
# """ 请求url，获取数据response """
# url = url.replace("unix_time", str(current_timestamp))
# res = requests.get(url).text
# print(res)
# res_p = re.sub("\\].*", "]", re.sub(".*:\\[", "[", res, 1), 1)
# try:
#     json_object = json.loads(res_p)
# except ValueError as e:
#     print("no more data")
for market in ["0", "1"]:
    for i in range(1, 100):
        print("i: ", i)
        url1 = url.replace("unix_time", str(current_timestamp)).replace(
            "market", market).replace("pn=i", "pn="+str(i))
        print(url1)
